
import React from 'react';
import { render } from '@testing-library/react';
import PodcastDetailCard from '../PodcastDetailCard';

test('renders the header component', () => {
  render(<PodcastDetailCard  />);
  // Add your assertions here
});
